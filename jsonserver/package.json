{
  "dependencies": {
    "json-server": "^0.17.1"
  },
  "scripts": {
    
    "server": "json-server --port 3003 --watch db.json --host 127.0.0.1",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
